mainmodule main {

		readonly CProxy_Main mainProxy;
		readonly int numChares; 

   		extern module Irrigacao;
   				mainchare Main {
			      entry Main (CkArgMsg* msg);
			      entry void retorno(double qot,double ero);
			      entry void solucaoOtima(double ero, double qot);
			      entry void terminado ();
  };
};